### Testing task 1:

# Carry out static testing on the code below.
# Comment on any errors that you see below.
```ruby
### Testing task 2 code:

# Carry out dynamic testing on the code below.
# Correct the errors below that you spotted in task 1.

require_relative('card.rb')
class CardGame



# error in naming capital A and needs ==
  def checkforAce(card)
    if card.value = 1
      return true
    else
      return false
    end
  end

# Def spelled wrong and no comma between card1 and card 2 and return should be suit
  dif highest_card(card1 card2)
  if card1.value > card2.value
    return card.name
  else
    card2
  end
end
end

# total hasn't assigned a value and no end to end class and added string interpolation
def self.cards_total(cards)
  total
  for card in cards
    total += card.value
    return "You have a total of" + total
  end
end

end
```
